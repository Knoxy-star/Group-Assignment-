spring:
  application:
    name: dashboard-service

server:
  port: 8089

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true