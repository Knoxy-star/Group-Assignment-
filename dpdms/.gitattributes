# Normalize line endings so Windows/Mac/Linux teammates don't produce
# noisy whole-file diffs just from CRLF vs LF differences.
* text=auto eol=lf

*.jar binary
*.zip binary
*.png binary
*.jpg binary
